/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SqkmPipe } from './sqkm.pipe';

describe('Pipe: Sqkme', () => {
  it('create an instance', () => {
    let pipe = new SqkmPipe();
    expect(pipe).toBeTruthy();
  });
});
